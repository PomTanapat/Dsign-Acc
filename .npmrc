# next@15.0.3 peer-allows react ^18.2.0 or a pinned 19.0.0-rc snapshot, but
# this project (Phases 0-5) was built on react ^19 stable. Without this flag
# a clean `npm install` fails on ERESOLVE — locally and on Railway.
legacy-peer-deps=true
